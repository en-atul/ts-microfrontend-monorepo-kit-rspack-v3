# Enable automatic installation of missing peer dependencies
auto-install-peers=true

# Ensure the lockfile is generated (pnpm-lock.yaml)
lockfile=true

# Link workspace packages to each other
link-workspace-packages=true

# Use exact versions of dependencies
save-exact=true

# Ignore strict peer dependencies (don't fail on unmet peer dependencies)
strict-peer-dependencies=false
